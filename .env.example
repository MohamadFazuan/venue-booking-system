# Copy this file to .env and fill in values
# Never commit .env to version control

# App
VITE_APP_VERSION=1.0.0
VITE_APP_NAME=VenueKL

# Port for docker-compose
PORT=8080

# Future: API base URL (no backend yet)
# VITE_API_BASE_URL=https://api.venuekl.com/v1

# Future: Auth secret (server-side only — never prefix with VITE_)
# SESSION_SECRET=change-me-in-production
